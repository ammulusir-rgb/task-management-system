/**
 * Project Detail component types and i18n keys
 */
export interface ProjectDetailI18nKeys {
  noDescription: string;
  reports: string;
  archive: string;
  restore: string;
  boards: string;
  columns: string;
  defaultBoard: string;
  archived: string;
  restored: string;
}

export const PROJECT_DETAIL_I18N_KEYS: ProjectDetailI18nKeys = {
  noDescription: 'projects.noDescription',
  reports: 'projects.reports',
  archive: 'projects.archive',
  restore: 'projects.restore',
  boards: 'projects.boards',
  columns: 'projects.columns',
  defaultBoard: 'projects.default',
  archived: 'projects.archived',
  restored: 'projects.restored',
};
