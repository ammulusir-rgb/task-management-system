/**
 * Header component types and i18n keys
 */
export interface HeaderI18nKeys {
  profile: string;
  settings: string;
  signOut: string;
  searchPlaceholder: string;
  switchLanguage: string;
}

export const HEADER_I18N_KEYS: HeaderI18nKeys = {
  profile: 'header.profile',
  settings: 'header.settings',
  signOut: 'header.signOut',
  searchPlaceholder: 'shell.searchPlaceholder',
  switchLanguage: 'header.switchLanguage',
};
